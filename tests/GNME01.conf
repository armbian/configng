ENABLED=true
RELEASE="bookworm:trixie:noble"
TESTNAME="GNOME desktop install and remove"

testcase() {(
	set -e
	./bin/armbian-config --api module_desktop install de=gnome
	[ -f /etc/armbian/desktop/gnome.packages ]
	./bin/armbian-config --api module_desktop remove de=gnome
)}
