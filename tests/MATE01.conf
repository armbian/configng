ENABLED=true
RELEASE="bookworm:trixie:noble"
TESTNAME="Mate desktop install and remove"

testcase() {(
	set -e
	./bin/armbian-config --api module_desktop install de=mate
	./bin/armbian-config --api module_desktop remove de=mate
)}
