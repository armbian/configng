ENABLED=true
RELEASE="noble"
TESTNAME="Nextcloud install"

testcase() {(
	set -e
	./bin/armbian-config --api module_nextcloud purge
	./bin/armbian-config --api module_nextcloud install
	./bin/armbian-config --api module_nextcloud status
	./bin/armbian-config --api module_nextcloud purge
)}


