ENABLED=true
RELEASE="noble"
TESTNAME="Prometheus install"

testcase() {(
	set -e
	./bin/armbian-config --api module_prometheus purge
	./bin/armbian-config --api module_prometheus install
	./bin/armbian-config --api module_prometheus status
	./bin/armbian-config --api module_prometheus purge
)}


