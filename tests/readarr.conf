ENABLED=true
RELEASE="noble"
TESTNAME="Readarr install"

testcase() {(
	set -e
	./bin/armbian-config --api module_readarr purge
	./bin/armbian-config --api module_readarr install
	./bin/armbian-config --api module_readarr status
)}
