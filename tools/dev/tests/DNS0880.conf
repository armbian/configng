ENABLED=true
RELEASE="x86-64"
MENUID="DNS0880"

function testcase(){
		armbian-config --api module_unbound install
		[[ -n "$condition" ]]
		armbian-config --api module_unbound remove
		[[ -z "$condition" ]]
}
