ENABLED=true
RELEASE="x86-64 arm64"
MENUID="DOW0570"

function testcase(){
		armbian-config --api module_deluge install
		[[ -n "$condition" ]]
		armbian-config --api module_deluge remove
		[[ -z "$condition" ]]
}
