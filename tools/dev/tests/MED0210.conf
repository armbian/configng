ENABLED=true
RELEASE="x86-64 arm64"
MENUID="MED0210"

function testcase(){
		armbian-config --api module_stirling install
		[[ -n "$condition" ]]
		armbian-config --api module_stirling remove
		[[ -z "$condition" ]]
}
