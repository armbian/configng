ENABLED=true
RELEASE="Missing"
MENUID="MON0350"

function testcase(){
		armbian-config --api module_domoticz install
		[[ -n "$condition" ]]
		armbian-config --api module_domoticz remove
		[[ -z "$condition" ]]
}
