ENABLED=true
RELEASE="x86-64 arm64"
MENUID="DOW0800"

function testcase(){
		armbian-config --api module_medusa install
		[[ -n "$condition" ]]
		armbian-config --api module_medusa remove
		[[ -z "$condition" ]]
}
