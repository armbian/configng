ENABLED=true
RELEASE="Missing"
MENUID="HOM0490"

function testcase(){
		armbian-config --api module_evcc install
		[[ -n "$condition" ]]
		armbian-config --api module_evcc remove
		[[ -z "$condition" ]]
}
