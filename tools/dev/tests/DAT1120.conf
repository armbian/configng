ENABLED=true
RELEASE="x86-64 arm64"
MENUID="DAT1120"

function testcase(){
		armbian-config --api module_mariadb install
		[[ -n "$condition" ]]
		armbian-config --api module_mariadb remove
		[[ -z "$condition" ]]
}
