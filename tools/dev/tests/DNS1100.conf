ENABLED=true
RELEASE="Missing"
MENUID="DNS1100"

function testcase(){
		armbian-config --api module_pi_hole install
		[[ -n "$condition" ]]
		armbian-config --api module_pi_hole remove
		[[ -z "$condition" ]]
}
