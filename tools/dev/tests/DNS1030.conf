ENABLED=true
RELEASE="Missing"
MENUID="DNS1030"

function testcase(){
		armbian-config --api module_adguardhome install
		[[ -n "$condition" ]]
		armbian-config --api module_adguardhome remove
		[[ -z "$condition" ]]
}
