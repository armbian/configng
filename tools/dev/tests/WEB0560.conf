ENABLED=true
RELEASE="x86-64 arm64"
MENUID="WEB0560"

function testcase(){
		armbian-config --api module_swag install
		[[ -n "$condition" ]]
		armbian-config --api module_swag remove
		[[ -z "$condition" ]]
}
