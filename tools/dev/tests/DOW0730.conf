ENABLED=true
RELEASE="x86-64 arm64"
MENUID="DOW0730"

function testcase(){
		armbian-config --api module_transmission install
		[[ -n "$condition" ]]
		armbian-config --api module_transmission remove
		[[ -z "$condition" ]]
}
