ENABLED=true
RELEASE="x86-64 arm64"
MENUID="MED0690"

function testcase(){
		armbian-config --api module_embyserver install
		[[ -n "$condition" ]]
		armbian-config --api module_embyserver remove
		[[ -z "$condition" ]]
}
