ENABLED=true
RELEASE="x86-64 arm64 armhf"
MENUID="HOM0110"

function testcase(){
		armbian-config --api module_haos install
		[[ -n "$condition" ]]
		armbian-config --api module_haos remove
		[[ -z "$condition" ]]
}
