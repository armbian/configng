ENABLED=true
RELEASE="x86-64 arm64"
MENUID="DOW0660"

function testcase(){
		armbian-config --api module_radarr install
		[[ -n "$condition" ]]
		armbian-config --api module_radarr remove
		[[ -z "$condition" ]]
}
