ENABLED=true
RELEASE="x86-64 arm64"
MENUID="DOW0160"

function testcase(){
		armbian-config --api module_bazarr install
		[[ -n "$condition" ]]
		armbian-config --api module_bazarr remove
		[[ -z "$condition" ]]
}
