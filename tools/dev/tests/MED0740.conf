ENABLED=true
RELEASE="x86-64 arm64"
MENUID="MED0740"

function testcase(){
		armbian-config --api module_nextcloud install
		[[ -n "$condition" ]]
		armbian-config --api module_nextcloud remove
		[[ -z "$condition" ]]
}
