ENABLED=true
RELEASE="x86-64 arm64"
MENUID="MED0810"

function testcase(){
		armbian-config --api module_syncthing install
		[[ -n "$condition" ]]
		armbian-config --api module_syncthing remove
		[[ -z "$condition" ]]
}
