ENABLED=true
RELEASE="x86-64 arm64"
MENUID="DAT0200"

function testcase(){
		armbian-config --api module_phpmyadmin install
		[[ -n "$condition" ]]
		armbian-config --api module_phpmyadmin remove
		[[ -z "$condition" ]]
}
