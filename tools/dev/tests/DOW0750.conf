ENABLED=true
RELEASE="x86-64 arm64"
MENUID="DOW0750"

function testcase(){
		armbian-config --api module_navidrome install
		[[ -n "$condition" ]]
		armbian-config --api module_navidrome remove
		[[ -z "$condition" ]]
}
