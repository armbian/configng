ENABLED=true
RELEASE="x86-64 arm64"
MENUID="PRI0620"

function testcase(){
		armbian-config --api module_octoprint install
		[[ -n "$condition" ]]
		armbian-config --api module_octoprint remove
		[[ -z "$condition" ]]
}
