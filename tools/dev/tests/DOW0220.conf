ENABLED=true
RELEASE="x86-64 arm64"
MENUID="DOW0220"

function testcase(){
		armbian-config --api module_sonarr install
		[[ -n "$condition" ]]
		armbian-config --api module_sonarr remove
		[[ -z "$condition" ]]
}
