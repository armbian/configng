ENABLED=true
RELEASE="x86-64 arm64"
MENUID="NET0550"

function testcase(){
		armbian-config --api module_wireguard install
		[[ -n "$condition" ]]
		armbian-config --api module_wireguard remove
		[[ -z "$condition" ]]
}
